---
name: Block on variable
"on":
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  block-if-var:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the var
        run: | 
          echo "Hello the VAR is ${{ env.VAR }}"
